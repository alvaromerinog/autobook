generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Car {
  id           String    @id @default(uuid())
  brand        String
  model        String
  year         Int
  licensePlate String
  color        String?
  mileage      Int?
  deletedAt    DateTime?
}
